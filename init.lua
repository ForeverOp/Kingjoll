-- INIT PAI (motor)
-- NÃO coloque script aqui

local scriptUrl = ...

if not scriptUrl then
    error("Página Lua não definida")
end

local code = game:HttpGet(scriptUrl)
loadstring(code)()-- INIT PAI (motor)
-- NÃO coloque script aqui

local scriptUrl = ...

if not scriptUrl then
    error("Página Lua não definida")
end

local code = game:HttpGet(scriptUrl)
loadstring(code)()
